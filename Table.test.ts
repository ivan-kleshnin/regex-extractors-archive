// import {skillTable} from "../taxonomy"

describe("SkillTable()", () => {
  it("dummy", () => {
    expect(2).toBe(2)
  })

  // describe(".getNode()", () => {
  //   it("works", () => {
  //     expect(skillTable.getNode("Software")).toEqual(
  //       expect.objectContaining({name: "Software"})
  //     )
  //     expect(skillTable.getNode("Web")).toEqual(
  //       expect.objectContaining({name: "Web"})
  //     )
  //     expect(skillTable.getNode("Backend")).toEqual(
  //       expect.objectContaining({name: "Backend"})
  //     )
  //     expect(skillTable.getNode("PHP")).toEqual(
  //       expect.objectContaining({name: "PHP"})
  //     )
  //     expect(skillTable.getNode("QA")).toEqual(
  //       expect.objectContaining({name: "QA"})
  //     )
  //     expect(skillTable.getNode("Automation")).toEqual(
  //       expect.objectContaining({name: "Automation"})
  //     )
  //   })
  // })

  // describe(".getFlatNodes()", () => {
  //   it("works", () => {
  //     expect(skillTable.getFlatNodes("Software").map(n => n.name)).toEqual(
  //       expect.arrayContaining(["Web", "Mobile", "Game"])
  //     )
  //     expect(skillTable.getFlatNodes("Web").map(n => n.name)).toEqual(
  //       expect.arrayContaining(["Web", "Backend", "Laravel"])
  //     )
  //     expect(skillTable.getFlatNodes("Backend").map(n => n.name)).toEqual(
  //       expect.arrayContaining(["Laravel", "PHP"])
  //     )
  //     expect(skillTable.getFlatNodes("QA").map(n => n.name)).toEqual(
  //       expect.arrayContaining(["QA", "Selenium"])
  //     )
  //     expect(skillTable.getFlatNodes("Automation").map(n => n.name)).toEqual(
  //       expect.arrayContaining(["Automation", "Selenium"])
  //     )
  //   })
  // })
})
